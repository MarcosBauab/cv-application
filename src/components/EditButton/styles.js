import styled from 'styled-components';

export const Container = styled.div`

    position: relative;

    button.edit{
        position: absolute;

        top: -10px;
        right: -10px;
        cursor: pointer;
    }

`;
